class Solution {
    public String solution(int n) {
        String answer = String.valueOf(n); // 정수를 문자열로 변환
        // String answer = Integer.toString(n); // 정수를 문자열로 변환
        return answer;
    }
}
